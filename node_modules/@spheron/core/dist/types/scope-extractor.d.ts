import SpheronApi from "./spheron-api";
import { TokenScope } from "./spheron-api/interfaces";
import { AppTypeEnum } from "./spheron-api/enums";
declare abstract class ScopeExtractor {
    private readonly api;
    private scope;
    private lock;
    constructor(spheronApi: SpheronApi);
    protected getScopeFromToken(): Promise<TokenScope>;
    protected getOrganizationIdFromToken(): Promise<string>;
    protected getOrganizationTypeFromToken(): Promise<AppTypeEnum>;
}
export { ScopeExtractor };
